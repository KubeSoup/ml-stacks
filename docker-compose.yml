version: '3'

services:
  mlflow:
    image: 'ghcr.io/KubeSoup/ml-stacks/mlflow-docker'
    build: .
    ports:
      - "5000:5000"
    volumes:
      - "./data:/mlflow"
    environment:
      BACKEND_URI: sqlite:////mlflow/mlflow.db
      ARTIFACT_ROOT: /mlflow/artifacts
